[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
markers =
    integration: test requiring a Postgres database (reads DATABASE_URL; runs in CI, auto-skipped locally without a DB)
